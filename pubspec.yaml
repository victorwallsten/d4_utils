name: d4_utils
description: Utilities for D4
publish_to: 'none'
version: 0.1.2+3

environment:
  sdk: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
